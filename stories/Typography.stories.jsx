import React from 'react';

export default { title: 'Foundations/Typography' };

export const Headings = () => (
  <div>
    <h1 className="h1">Heading 1 example</h1>
    <h2>Heading 2 example</h2>
    <p className="body-text">Body copy example — font tokens reference Helvetica Now but fallback to system fonts.</p>
  </div>
);
